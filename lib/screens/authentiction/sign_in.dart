import 'package:flutter/material.dart';
class SignIn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          "kaziManager",
          style: TextStyle(color: Colors.black87),)


        
      ),
      body: Container(
        child: Center(child: Text("coming through"),)

      ),
      
    );
  }
}